export default function Home() {
  return (
    <main className="flex flex-col items-center justify-center min-h-screen bg-brand-dark">
      <h1 className="text-5xl font-medium text-white" style={{ fontFamily: "var(--font-heading)" }}>
        Sell Out Faster.
      </h1>
      <p className="mt-4 text-gray-400" style={{ fontFamily: "var(--font-body)" }}>
        wowdesign.io — build in progress
      </p>
    </main>
  );
}
